# Non-effective targeted characterization requirements

## Purpose and first gate

This document creates no bundle, activation, execution plan, serial authority,
DAC authority, or physical permission. The exact Stage 5 plan has now been
recovered and the separately frozen zero-I/O attempt has completed. All source
identities and the exact V1 baseline pass, but all three frozen equilibrium
models have empty complete identification sets at minimum, nominal, and
maximum retained gain.

The first gate is therefore operator authorization of a new prospective
characterization prompt. Until that distinct authority exists, do not create a
bundle, rehearse, open serial, reset, flash, arm, issue a DAC command, or enter
the bench path.

## Unresolved parameter relationship

The recovered-source comparator reconstructs 18 exact, non-overlapping
600-second Stage 5 supports. Their integer count-error sequence is
`28,27,2,3,-6,-6,2,2,28,28,55,54,72,73,55,55,28,29` in the predetermined
nine-dwell order. At each retained gain, the closed half-count observation
sets cannot share one equilibrium code. The nominal and maximum constant
models become empty at the first `lower_interior_1` support; the minimum-gain
case becomes empty at that dwell's second support.

The independently frozen `±1.91 codes/hour` drift cannot reconcile the exact
time sequence. Separating outbound/anchor and return histories with a common
return offset over the full frozen `[-8,+8]` codes also fails because the
outbound/anchor set is already empty. Thus equilibrium intercept, finite-run
gain, time drift, count quantization, and reversal history remain empirically
inconsistent under every allowed model. Attempt 4 stays held out and cannot
repair an empty identification set.

## Smallest replacement physical evidence

Prefer one finite, predetermined open-loop characterization over a chain of
micro-runs. It should reuse the existing capture-owned predetermined open-loop
executor, evidence snapshot, analyzer, sealing, and registration path, but only
under a new exact bundle and explicit later authority.

At the scientific-requirements level, freeze three interior codes before entry:

- lower: 43046 (`0xA826`);
- centre: 43070 (`0xA83E`); and
- upper: 43094 (`0xA856`).

All are inside the established `0xA800..0xAB00` envelope. The 24-code adjacent
spacing gives at least `0.0039257813 Hz` differential response at minimum gain,
above the retained approximately `2/600 Hz` empirical detection floor. The
centre is a characterization coordinate from retained evidence, not a claimed
true equilibrium or setup authority.

Use one prospectively frozen 12-dwell order:

```text
C, L, C, U, C, L, C, U, C, L, C, U
```

The first seven complete dwell segments form identification evidence; the last
five form held-out validation. This supplies repeated same-code centre returns,
lower and upper departures, both reversal directions, and later repeats without
splitting adjacent supports between partitions. Commands remain predetermined;
no observed frequency or phase result may alter the order.

Each dwell requires 900 seconds settling exclusion followed by three fresh,
contiguous, non-overlapping 600-second D14-qualified D8 supports: 2,700 seconds
per dwell. Three supports permit an exact 1,800-second aggregate whose one-count
span maps to about 3.40 codes at minimum gain, leaving explicit room inside the
frozen 18-code usefulness limit for drift and history sensitivity. Twelve
dwells require 32,400 seconds after the first application. With a separately
frozen 1,800-second initial identity/warmup interval, the minimum scientific
duration is 34,200 seconds, excluding bounded command and finalization slack.

## Required retained records

The replacement package must retain and identity-bind:

- the exact prospective plan, source revision, build profile, compile-time
  configuration, binary, host tools, and stop contract;
- D14 reference observations and accepted-boundary identity;
- D8 cumulative counts and all twelve sets of three exact 600-second supports;
- every requested and acknowledged AD5693R code, application tick, DAC epoch,
  settling boundary, and first dependent estimator support;
- response records for every transition and same-code return;
- capture session, exact counter domain, support first/last sequences, reset,
  invalidity, gap, duplicate, and transport health;
- SHT41 nearby-air temperature as a labeled covariate, including missing and
  stale status, without treating it as CX317 internal temperature;
- raw evidence, immutable manifest, analyzer and report identities, seal, and
  registered content digest; and
- arbitrary-epoch raw relative phase only if captured for same-epoch
  consistency. It is not required to identify equilibrium and may not be joined
  across epochs.

D10 is neither required nor eligible. GNSS serial metadata remains
qualification-only and cannot replace D14.

## Prospective analysis and success rules

Before any replacement acquisition, freeze the same three model hypotheses,
exact interval arithmetic, 18-code usefulness threshold, partition, nuisance
cases, and terminal precedence used in this study. Success requires:

- every identity and raw package validation passes with zero unexplained
  missing source;
- lower and upper evidence bracket zero frequency relative to D14 under at
  least one structurally identifiable positive-gain model;
- the identification complete feasible set is nonempty;
- all five held-out dwell segments have 100% predictive coverage within the
  prospectively frozen residual interval;
- minimum, nominal, maximum gain, half-count boundaries, positive and negative
  one-count same-code perturbations, slow drift, both reversal histories,
  settling boundaries, and leave-one-segment-out cases remain nonempty;
- the worst complete equilibrium interval spans at most 18 codes and is
  materially narrower than the 768-code characterized range; and
- provenance remains exact through the first dependent prediction decision.

An empty, unbounded, wider-than-18-code, history-ambiguous, held-out-failing, or
sensitivity-failing result is a valid not-observable terminal. Do not add a
model, change a nuisance, move the split, or widen the threshold after seeing
evidence.

## Invalidity and physical stop requirements

The replacement study is invalid for a missing/mismatched plan, manifest,
binary, tool, source record, DAC epoch, support identity, counter domain,
partition boundary, or exact baseline. A future physical programme must also
freeze fail-static stop conditions for reference/capture loss, impossible
ordering, acknowledgement failure, range breach, unexpected code, transport
obstruction, and evidence discontinuity. It must preserve an independent
bounded abort and the last confirmed DAC state.

No execution, bundle, activation, rehearsal, or physical authority is created
here. The current programme remains inactive and all physical-authority fields
remain false.
