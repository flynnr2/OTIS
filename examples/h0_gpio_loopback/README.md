# H0 GPIO Loopback Run Template

Template for `SW1_GPIO_LOOPBACK` captures. It is intentionally empty except for
CSV headers and must not be treated as captured hardware data.

Expected wiring for this mode is `D7` output jumpered to `D10` / `CH0`.
