# H0 GPS PPS Run Template

Template for `SW1_GPS_PPS` captures. It is intentionally empty except for CSV
headers and must not be treated as captured hardware data.

Expected wiring is Adafruit Ultimate GPS PPS to `D14` / `CH1`, with voltage and
edge conditioning suitable for RP2040 GPIO.
