# H0 PPS + TCXO Synthetic Fixture

Synthetic fixture used by the host tests and smoke commands. It is a known-good
contract example, not evidence from live H0 hardware.

Keep this fixture stable so `validate_run` and `report_run` have a compact
baseline while SW1 live bring-up evolves.
